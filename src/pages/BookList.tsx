import { FC, JSX } from "react"

interface props {}

const BookList: FC<props> = (): JSX.Element => {
    return (
        <div>
            {"BookList"}
        </div>
    )
}

export default BookList